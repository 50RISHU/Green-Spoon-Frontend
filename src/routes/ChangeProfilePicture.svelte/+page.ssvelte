<script> import { goto } from '$app/navigation'; export let initialName = 'Chirag Prajapati'; let name = initialName; let avatarSeed = name.split(' ')[0] || 'user'; let message = ''; let error = ''; function handleSubmit(event) { event.preventDefault(); if (!name.trim()) { error = '⚠️ Name is required!'; message = ''; return; } avatarSeed = name.trim().split(' ')[0] || 'user'; message = '✅ Profile picture updated!'; error = ''; setTimeout(() => { goto('/profile'); }, 1500); } </script> <div class="container my-5"> <div class="row justify-content-center"> <div class="col-md-6"> <div class="card shadow-sm animate__animated animate__zoomIn"> <div class="card-body text-center"> <h4 class="text-success mb-3">🖼️ Change Profile Picture</h4>

      {#if error}
        <div class="alert alert-danger">{error}</div>
      {/if}
      {#if message}
        <div class="alert alert-success">{message}</div>
      {/if}

      <form on:submit={handleSubmit}>
        <div class="mb-4">
          <img
            src={`https://api.dicebear.com/7.x/initials/svg?seed=${avatarSeed}`}
            alt="Avatar Preview"
            class="rounded-circle"
            style="width: 100px; height: 100px;"
          />
        </div>

        <div class="mb-3 text-start">
          <label class="form-label fw-bold">Enter Your Name</label>
          <input
            type="text"
            class="form-control"
            bind:value={name}
            placeholder="Full Name"
          />
        </div>

        <button type="submit" class="btn btn-success w-100">
          🔁 Update Picture
        </button>

        <div class="mt-3">
          <a href="/profile" class="text-decoration-none text-muted">← Back to Profile</a>
        </div>
      </form>
    </div>
  </div>
</div>
</div> </div> <style> @import "https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"; :global(body) { background-color: #d1ffbd; padding-bottom: 80px; } </style>